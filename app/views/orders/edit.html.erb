<h1>Editing order</h1>
<%= render 'form' %> 
<!-- <%= form_for :order, url: order_path(@order), method: :patch do |f| %>
 
  <% if @order.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@order.errors.count, "error") %> prohibited you from placing your order:
      </h2>
      <ul>
        <% @order.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
 
  <p>
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </p>
 
  <p>
    <%= f.label :item %><br>
    <%= f.text_area :item %>
  </p>
 
  <p>
    <%= f.submit %>
  </p>
 
<% end %> -->
<%= link_to 'Menu', orders_path, class: 'btn btn-primary btn-xs' %>  |
<%= link_to 'Back', orders_path, class: 'btn btn-primary btn-xs' %>
